using System;
/*
# The static keyword is used to declare a member that belongs to the class itself rather than to a specific object.
# A static class is a class that cannot be instantiated and can only contain static members.
# A static constructor is a constructor that is called only once when the class is first loaded into memory.
*/

 public class StaticApp {
    public static void Main (string[] args) {

    }
 }